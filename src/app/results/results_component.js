import template from './results.html';
import './results.less';

const controller = function () {
    this.name = 'Katy';
};

export default {
    template,
    controller,
    controllerAs: 'vm',
    bindings: {}
};
